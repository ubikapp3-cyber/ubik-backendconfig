server:
  port: 8084

spring:
  application:
    name: reservation-management-service

  # PostgreSQL R2DBC
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/reservation_management_db
    username: postgres
    password: carlosmanuel
    pool:
      initial-size: 10
      max-size: 20
      max-idle-time: 30m

  sql:
    init:
      mode: always
      platform: postgresql

# JWT Configuration
jwt:
  secret: (la_misma_de_julian)
  expiration: 86400000

# Microservicios externos
microservices:
  user-service:
    url: http://localhost:8081
    timeout: 5000
  motel-service:
    url: http://localhost:8083
    timeout: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.ubik.reservation: DEBUG
    org.springframework.r2dbc: DEBUG
    org.springframework.security: DEBUG