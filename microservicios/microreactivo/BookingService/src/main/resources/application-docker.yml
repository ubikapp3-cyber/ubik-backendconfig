server:
  port: 8084

spring:
  application:
    name: reservation-management-service

  r2dbc:
    url: r2dbc:postgresql://postgres-reservations:5432/reservation_management_db
    username: postgres
    password: carlosmanuel
    pool:
      initial-size: 10
      max-size: 20
      max-idle-time: 30m

  sql:
    init:
      mode: never

jwt:
  secret: mySecretKey1234567890abcdef1234567890abcdef
  expiration: 86400000

microservices:
  user-service:
    url: http://user-management-app:8081
    timeout: 5000
  motel-service:
    url: http://motel-management-app:8083
    timeout: 5000

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.ubik.reservation: INFO